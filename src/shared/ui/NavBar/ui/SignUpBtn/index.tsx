import React, { FC } from 'react';

import styles from './styles.module.scss';

const SignUpBtn: FC = () => {
	return <button className={styles.SignUpBtn}>Sign In</button>;
};

export default SignUpBtn;
