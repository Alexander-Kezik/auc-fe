import React, { FC } from 'react';

import styles from './styles.module.scss';

const SignInBtn: FC = () => {
	return <button className={styles.SignInBtn}>Sign In</button>;
};

export default SignInBtn;
